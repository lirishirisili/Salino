<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".SalinoApp"
        android:allowBackup="true"
        android:icon="@drawable/ic_salino_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_salino_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.Salino">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Salino">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- TODO: Add FCM service when implementing push notifications
        <service
            android:name=".data.remote.SalinoFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
        -->

    </application>
</manifest>
